"use client";
import {
  DevtoolsProvider,
  PiPProvider,
  Portal,
  createComponent,
  initialState,
  lazy,
  render
} from "./chunk-IKOLPKLM.js";
import {
  require_jsx_runtime
} from "./chunk-2YVA4HRZ.js";
import {
  require_react_dom
} from "./chunk-YF4B4G2L.js";
import {
  require_react
} from "./chunk-WUR7D6NS.js";
import {
  __toESM
} from "./chunk-G3PMV62Z.js";

// node_modules/@tanstack/react-devtools/dist/esm/devtools.js
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var import_react = __toESM(require_react(), 1);

// node_modules/@tanstack/devtools/node_modules/@tanstack/devtools-event-bus/dist/esm/utils/json.js
function stringifyWithBigInt(data) {
  return JSON.stringify(data, (_key, value) => {
    if (typeof value === "bigint") {
      return {
        __type: "bigint",
        value: value.toString()
      };
    }
    return value;
  });
}
function parseWithBigInt(json) {
  return JSON.parse(json, (_key, value) => {
    if (value && typeof value === "object" && value.__type === "bigint" && typeof value.value === "string") {
      return BigInt(value.value);
    }
    return value;
  });
}

// node_modules/@tanstack/devtools/node_modules/@tanstack/devtools-event-bus/dist/esm/client/client.js
function getDefaultPort(configPort) {
  if (typeof __TANSTACK_DEVTOOLS_PORT__ !== "undefined")
    return __TANSTACK_DEVTOOLS_PORT__;
  return configPort;
}
var ClientEventBus = class {
  #port;
  #socket;
  #eventSource;
  #eventTarget;
  #debug;
  #connectToServerBus;
  #broadcastChannel;
  #dispatcher = (e) => {
    const event = e.detail;
    this.emitToServer(event);
    this.emitToClients(event);
  };
  #connectFunction = () => {
    this.debugLog(
      "Connection request made to event-bus, replying back with success"
    );
    this.#eventTarget.dispatchEvent(new CustomEvent("tanstack-connect-success"));
  };
  constructor({
    port = 4206,
    debug = false,
    connectToServerBus = false
  } = {}) {
    this.#debug = debug;
    this.#broadcastChannel = new BroadcastChannel("tanstack-devtools");
    this.#eventSource = null;
    this.#port = getDefaultPort(port);
    this.#socket = null;
    this.#connectToServerBus = connectToServerBus;
    this.#eventTarget = this.getGlobalTarget();
    this.#broadcastChannel.onmessage = (e) => {
      this.emitToClients(parseWithBigInt(e.data), true);
    };
    this.debugLog("Initializing client event bus");
  }
  emitToClients(event, fromBroadcastChannel = false) {
    this.debugLog("Emitting event from client bus", event);
    const specificEvent = new CustomEvent(event.type, { detail: event });
    this.debugLog("Emitting event to specific client listeners", event);
    this.#eventTarget.dispatchEvent(specificEvent);
    const globalEvent = new CustomEvent("tanstack-devtools-global", {
      detail: event
    });
    if (!fromBroadcastChannel) {
      this.#broadcastChannel?.postMessage(stringifyWithBigInt(event));
    }
    this.debugLog("Emitting event to global client listeners", event);
    this.#eventTarget.dispatchEvent(globalEvent);
  }
  emitToServer(event) {
    const json = stringifyWithBigInt(event);
    if (this.#socket && this.#socket.readyState === WebSocket.OPEN) {
      this.debugLog("Emitting event to server via WS", event);
      this.#socket.send(json);
    } else if (this.#eventSource) {
      this.debugLog("Emitting event to server via SSE", event);
      fetch(`http://localhost:${this.#port}/__devtools/send`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: json
      }).catch(() => {
      });
    }
  }
  start() {
    this.debugLog("Starting client event bus");
    if (typeof window === "undefined") {
      return;
    }
    if (this.#connectToServerBus) {
      this.connect();
    }
    this.#eventTarget = window;
    this.#eventTarget.addEventListener(
      "tanstack-dispatch-event",
      this.#dispatcher
    );
    this.#eventTarget.addEventListener(
      "tanstack-connect",
      this.#connectFunction
    );
  }
  stop() {
    this.debugLog("Stopping client event bus");
    if (typeof window === "undefined") {
      return;
    }
    this.#eventTarget.removeEventListener(
      "tanstack-dispatch-event",
      this.#dispatcher
    );
    this.#eventTarget.removeEventListener(
      "tanstack-connect",
      this.#connectFunction
    );
    this.#eventSource?.close();
    this.#socket?.close();
    this.#socket = null;
    this.#eventSource = null;
  }
  getGlobalTarget() {
    if (typeof window !== "undefined") {
      return window;
    }
    return new EventTarget();
  }
  debugLog(...messages) {
    if (this.#debug) {
      console.log("🌴 [tanstack-devtools:client-bus]", ...messages);
    }
  }
  connectSSE() {
    this.debugLog("Connecting to SSE server");
    this.#eventSource = new EventSource(
      `http://localhost:${this.#port}/__devtools/sse`
    );
    this.#eventSource.onmessage = (e) => {
      this.debugLog("Received message from SSE server", e.data);
      this.handleEventReceived(e.data);
    };
  }
  connectWebSocket() {
    this.debugLog("Connecting to WebSocket server");
    this.#socket = new WebSocket(`ws://localhost:${this.#port}/__devtools/ws`);
    this.#socket.onmessage = (e) => {
      this.debugLog("Received message from server", e.data);
      this.handleEventReceived(e.data);
    };
    this.#socket.onclose = () => {
      this.debugLog("WebSocket connection closed");
      this.#socket = null;
    };
    this.#socket.onerror = () => {
      this.debugLog("WebSocket connection error");
    };
  }
  connect() {
    try {
      this.connectWebSocket();
    } catch {
      if (typeof window === "undefined") return;
      this.connectSSE();
    }
  }
  handleEventReceived(data) {
    try {
      const event = parseWithBigInt(data);
      this.emitToClients(event);
    } catch {
    }
  }
};

// node_modules/@tanstack/devtools/dist/dev.js
var TanStackDevtoolsCore = class {
  #config = {
    ...initialState.settings
  };
  #plugins = [];
  #isMounted = false;
  #dispose;
  #Component;
  #eventBus;
  #eventBusConfig;
  #setPlugins;
  constructor(init) {
    this.#plugins = init.plugins || [];
    this.#eventBusConfig = init.eventBusConfig;
    this.#config = {
      ...this.#config,
      ...init.config
    };
  }
  mount(el) {
    if (this.#isMounted) {
      throw new Error("Devtools is already mounted");
    }
    const mountTo = el;
    const dispose = render(() => {
      const _self$ = this;
      this.#Component = lazy(() => import("./RZMDLR3T-D77UDR3D.js"));
      const Devtools = this.#Component;
      this.#eventBus = new ClientEventBus(this.#eventBusConfig);
      this.#eventBus.start();
      return createComponent(DevtoolsProvider, {
        get plugins() {
          return _self$.#plugins;
        },
        get config() {
          return _self$.#config;
        },
        onSetPlugins: (setPlugins) => {
          _self$.#setPlugins = setPlugins;
        },
        get children() {
          return createComponent(PiPProvider, {
            get children() {
              return createComponent(Portal, {
                mount: mountTo,
                get children() {
                  return createComponent(Devtools, {});
                }
              });
            }
          });
        }
      });
    }, mountTo);
    this.#isMounted = true;
    this.#dispose = dispose;
  }
  unmount() {
    if (!this.#isMounted) {
      throw new Error("Devtools is not mounted");
    }
    this.#eventBus?.stop();
    this.#dispose?.();
    this.#isMounted = false;
  }
  setConfig(config) {
    this.#config = {
      ...this.#config,
      ...config
    };
    if (config.plugins) {
      this.#plugins = config.plugins;
      if (this.#isMounted && this.#setPlugins) {
        this.#setPlugins(config.plugins);
      }
    }
  }
};

// node_modules/@tanstack/react-devtools/dist/esm/devtools.js
var import_react_dom = __toESM(require_react_dom(), 1);
var convertRender = (Component, setComponents, e, theme) => {
  const element = typeof Component === "function" ? Component(e, theme) : Component;
  setComponents((prev) => ({
    ...prev,
    [e.getAttribute("id")]: element
  }));
};
var convertTrigger = (Component, setComponent, e, props) => {
  const element = typeof Component === "function" ? Component(e, props) : Component;
  setComponent(element);
};
var TanStackDevtools = ({
  plugins,
  config,
  eventBusConfig
}) => {
  const devToolRef = (0, import_react.useRef)(null);
  const [pluginContainers, setPluginContainers] = (0, import_react.useState)({});
  const [titleContainers, setTitleContainers] = (0, import_react.useState)({});
  const [triggerContainer, setTriggerContainer] = (0, import_react.useState)(
    null
  );
  const [PluginComponents, setPluginComponents] = (0, import_react.useState)({});
  const [TitleComponents, setTitleComponents] = (0, import_react.useState)({});
  const [TriggerComponent, setTriggerComponent] = (0, import_react.useState)(
    null
  );
  const pluginsMap = (0, import_react.useMemo)(
    () => plugins?.map((plugin) => {
      return {
        ...plugin,
        name: typeof plugin.name === "string" ? plugin.name : (e, theme) => {
          const id = e.getAttribute("id");
          const target = e.ownerDocument.getElementById(id);
          if (target) {
            setTitleContainers((prev) => ({
              ...prev,
              [id]: e
            }));
          }
          convertRender(
            plugin.name,
            setTitleComponents,
            e,
            theme
          );
        },
        render: (e, theme) => {
          const id = e.getAttribute("id");
          const target = e.ownerDocument.getElementById(id);
          if (target) {
            setPluginContainers((prev) => ({
              ...prev,
              [id]: e
            }));
          }
          convertRender(plugin.render, setPluginComponents, e, theme);
        }
      };
    }) ?? [],
    [plugins]
  );
  const [devtools] = (0, import_react.useState)(() => {
    const { customTrigger, ...coreConfig } = config || {};
    return new TanStackDevtoolsCore({
      config: {
        ...coreConfig,
        customTrigger: customTrigger ? (el, props) => {
          setTriggerContainer(el);
          convertTrigger(customTrigger, setTriggerComponent, el, props);
        } : void 0
      },
      eventBusConfig,
      plugins: pluginsMap
    });
  });
  (0, import_react.useEffect)(() => {
    devtools.setConfig({
      plugins: pluginsMap
    });
  }, [devtools, pluginsMap]);
  (0, import_react.useEffect)(() => {
    if (devToolRef.current) {
      devtools.mount(devToolRef.current);
    }
    return () => devtools.unmount();
  }, [devtools]);
  const hasPlugins = Object.values(pluginContainers).length > 0 && Object.values(PluginComponents).length > 0;
  const hasTitles = Object.values(titleContainers).length > 0 && Object.values(TitleComponents).length > 0;
  return (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
    (0, import_jsx_runtime.jsx)("div", { style: { position: "absolute" }, ref: devToolRef }),
    hasPlugins ? Object.entries(pluginContainers).map(
      ([key, pluginContainer]) => (0, import_react_dom.createPortal)((0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: PluginComponents[key] }), pluginContainer)
    ) : null,
    hasTitles ? Object.entries(titleContainers).map(
      ([key, titleContainer]) => (0, import_react_dom.createPortal)((0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: TitleComponents[key] }), titleContainer)
    ) : null,
    triggerContainer && TriggerComponent ? (0, import_react_dom.createPortal)((0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: TriggerComponent }), triggerContainer) : null
  ] });
};

// node_modules/@tanstack/react-devtools/dist/esm/index.js
var TanStackDevtools2 = TanStackDevtools;
export {
  TanStackDevtools2 as TanStackDevtools
};
//# sourceMappingURL=@tanstack_react-devtools.js.map
